services:
  - type: web
    name: schedule-app
    runtime: node
    buildCommand: pnpm install --frozen-lockfile
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "3456"
      - key: DATABASE_URL
        sync: false
